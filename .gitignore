# Dados de runtime (estado do usuário — local, não versionar)
dados.json
dados.backup.json
Relatorios/
Backups/

# Arquivos arquivados (entulho movido na limpeza)
_arquivo_morto/

# Config local do Claude/preview
.claude/

# Backups pontuais
*.bak.json

# Relatórios .xls enviados pela app (dados de runtime)
entrada_detalhada.xls
entrada_resumida.xls

# Config com senha + arquivo de acesso da rede (não versionar)
config.json
ACESSO_REDE.txt

# Banco de atividades (runtime, local)
atividade.db

# Sessões ativas (runtime, local)
sessoes.json

# Multi-filial (dados de runtime, por filial)
filiais/
filiais.json

# Python
__pycache__/
*.pyc

# Sistema
Thumbs.db
desktop.ini
.DS_Store
